export default function Loading() {
    return(
        <main className="text-center">  
              <h2 className="text-primary">Cargando...</h2>
              <p>Esperemos que no tarde mucho :)</p>
        </main>
    );
}